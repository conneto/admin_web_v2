import { UserLoginRequest } from './user-login-request.model';

describe('UserLoginRequest', () => {
  it('should create an instance', () => {
    expect(new UserLoginRequest()).toBeTruthy();
  });
});
